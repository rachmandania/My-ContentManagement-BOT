name: Generate YouTube Video

on:
  schedule:
    # 1. Indonesia Target: 4:00 PM WIB (09:00 UTC)
    - cron: '0 9 * * *'
    # 2. US Target: 2:00 PM EST / 2:00 AM WIB (19:00 UTC)
    - cron: '0 19 * * *'
    
  workflow_dispatch: # Keeps your manual "Run workflow" button active

jobs:
  build-video:
    runs-on: ubuntu-latest
    # ... rest of your workflow file stays exactly the same
